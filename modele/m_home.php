<?php
    // Si aucun jeu n'est demandé, on affiche tous les matchs. Si un jeu est demandé,
    // on filtre les résultats pour ce jeu.
   /* try {
            $stmt = $bdd->query('SELECT * FROM evenements');
    }
    catch (PDOException $e) {
        die($e->getMessage());
    }*/
?>